// This is a TypeScript file that serves as a fallback for GitHub Pages
// It redirects to the properly built application when accessed directly

// Use JavaScript redirect to the main app
window.location.replace('/ekg-app/'); 